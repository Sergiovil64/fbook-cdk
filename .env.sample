# AWS account and region — required by CDK before synthesizing or deploying.
# Run: aws sts get-caller-identity --query Account --output text
CDK_DEFAULT_ACCOUNT=123456789012
CDK_DEFAULT_REGION=us-east-1
